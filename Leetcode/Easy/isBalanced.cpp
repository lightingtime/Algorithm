//
// Created by HCY on 2018/5/8.
//

#include "easyHeader.h"

//bool isBalanced(TreeNode* root) {
//    if (root == NULL) {
//        return true;
//    }
//    return (abs(maxDepth(root->left)-maxDepth(root->right))<=1) && isBalanced(root->left) && isBalanced(root->right);
//}

